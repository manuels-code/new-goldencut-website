import React from "react";
import Hero from "../hero.component";
import OurHistory from "../ourHistory.component";
const Home = () => {
  return (
    <div>
      <Hero />
      <OurHistory />
    </div>
  );
};

export default Home;
